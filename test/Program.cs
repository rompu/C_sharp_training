using System;
using System.Linq;
using System.Collections.Generic;

namespace test
{
    class Program
    {
        static void Main(string[] args)
        {
            var list = new List<int>() { 1, 2, 3, 4 };

            var limited = list.Where

            foreach (int number in limited)
                Console.WriteLine(number);
        }
    }
}
